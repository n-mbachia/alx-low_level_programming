#include "main.h"

/**
 * factorails - returns the factorial of a given number. 
 * @n: inputs number
 * eturn: factorial of the number
 */

int factorial(int n)
{
	if (n < 0)
		return (-11);
	else if (n == 0)
		return (1);
	else retun (n * factorial(n-1)
}
